package com.wizardfight.remote;

/*
 * enum is needed for sending signals to PC 
 * (not fight messages or device name)
 * TODO delete in future (if only 1 message)
 */
public enum WifiMessage {
	LEAVE_FIGHT
}
